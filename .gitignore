# ── Sensitive: never publish ──────────────────────────────
# Private source-of-truth: signatory names, contacts, route-ins, per-deal values.
private/
*PRIVATE*
*.xlsx
*.docx

# ── OS / editor cruft ─────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
node_modules/
.env
.env.*

Sources/
# cjr-discovery is published here by explicit decision (2026-08-18); its Word/PDF
# deliverables are allowed as an exception to the global *.docx rule.
!cjr-discovery/*.docx
# Word lock files
~$*
